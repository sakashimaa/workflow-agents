<template><div class="page-wrap pb-28 lg:pb-8"><h1 class="text-3xl font-black">Клиенты</h1><div class="panel mt-6 divide-y divide-slate-100"><NuxtLink v-for="customer in customers" :key="customer.id" :to="`/customers/${customer.id}`" class="flex items-center gap-4 p-5 hover:bg-slate-50"><span class="grid size-10 place-items-center rounded-xl bg-indigo-50 font-black text-indigo-600">{{ customer.company.slice(0, 1) }}</span><span><strong class="block">{{ customer.company }}</strong><small class="text-slate-500">{{ customer.name }}</small></span><span class="ml-auto text-slate-300">→</span></NuxtLink></div></div></template>
<script setup lang="ts">import type { CustomerSummary } from '#shared/types/domain'; definePageMeta({ middleware: 'staff' }); useSeoMeta({ title: 'Клиенты' }); const { data } = await useFetch<CustomerSummary[]>('/api/customers'); const customers = computed(() => data.value ?? [])</script>
